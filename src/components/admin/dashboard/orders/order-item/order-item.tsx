import React from 'react';

import { Order } from 'helper/schema/order';

type Props = {
    order: Order;
};

const OrderItem: React.FC<Props> = ({ order }) => {
    return <></>;
};

export { OrderItem };
